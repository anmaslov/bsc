if ($('#cart tr').length > 2) {
    $('#cart').show('blind', 1000);
} else {
    $('#bascet').modal('hide');
}

$('#cart').html("<%= escape_javascript render(@cart) %>");
$('#order_cart').html("<%= escape_javascript render :partial => 'carts/cart_order', :object => @cart %>");